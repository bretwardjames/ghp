# Save Work Session Context

Save the current work session context by adding a comment to the active issue. Use the `add_comment` MCP tool to document what was accomplished.

Summarize the current progress and any important context for future reference.

$ARGUMENTS
